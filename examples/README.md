# Examples

Only sanitized examples belong in this directory.

The public example is `examples/demo-basic/`. Private, generated, or book-derived examples should stay outside git or in ignored local directories. The root `.gitignore` intentionally ignores every `examples/*` path except this README and `examples/demo-basic/**`.
